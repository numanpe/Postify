# PostgreSQL connection used by Prisma.
# Local dev default matches docker-compose.yml.
DATABASE_URL="postgresql://postify:postify@localhost:5432/postify?schema=public"

# Auth.js session encryption secret. Generate with: npx auth secret
AUTH_SECRET=""

# --- Provider keys (all optional / BYOK) -----------------------------------
# The app must run without any of these set (free/local path).
# They are read server-side only — never expose these to the client.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# ELEVENLABS_API_KEY=
# RUNWAY_API_KEY=
